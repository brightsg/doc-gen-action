You are generating a getting-started guide for developers setting up this project for the first time.

## Required Sections

1. **Prerequisites** — required SDK versions, tools, accounts
2. **Setup** — step-by-step: clone, install dependencies, configure environment
3. **Local Development** — how to run the project locally (docker-compose, environment variables, ports)
4. **Running Tests** — commands for unit, integration, and E2E tests, and what infrastructure they need
5. **Key Entry Points** — where to start reading the code, main classes and their purposes
6. **Common Tasks** — how to add a new endpoint, create a migration, add a new service

## Rules

- Every command must be copy-pasteable
- Include expected output or success criteria for each step
- Note any gotchas or common setup issues
- Extract SDK versions from project files, Docker images from docker-compose
